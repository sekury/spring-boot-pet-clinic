subprojects {

    apply plugin: 'java'
    apply plugin: 'eclipse'

    group = 'sekury'
    version = '0.0.1-SNAPSHOT'
    sourceCompatibility = 10

    ext {
        springBootVersion = '2.0.4.RELEASE'
    }
    repositories {
        mavenCentral()
    }
}